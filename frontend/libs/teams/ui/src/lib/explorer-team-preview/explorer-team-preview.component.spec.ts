import '@angular/compiler';
import * as SUT from './explorer-team-preview.component';

describe('explorer-team-preview.component', () => {
	it('should be defined', () => {
		expect(SUT).toBeDefined();
	});
});
