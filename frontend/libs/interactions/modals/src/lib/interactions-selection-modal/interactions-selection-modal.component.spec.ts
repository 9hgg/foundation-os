import { DEFAULT_INTERACTION_SELECTION_MODAL_DATA, InteractionsSelectionModalComponent } from './interactions-selection-modal.component';

describe('interactions-selection-modal.component', () => {
	describe('DEFAULT_INTERACTION_SELECTION_MODAL_DATA', () => {
		it('is exported', () => {
			expect(DEFAULT_INTERACTION_SELECTION_MODAL_DATA).toBeDefined();
		});
	});

	describe('InteractionsSelectionModalComponent', () => {
		it('is exported', () => {
			expect(InteractionsSelectionModalComponent).toBeDefined();
		});
	});
});
