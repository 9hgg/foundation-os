import { ContributorPillComponent } from './contributor-pill.component';

describe('contributor-pill.component', () => {
	describe('ContributorPillComponent', () => {
		it('is exported', () => {
			expect(ContributorPillComponent).toBeDefined();
		});
	});
});
