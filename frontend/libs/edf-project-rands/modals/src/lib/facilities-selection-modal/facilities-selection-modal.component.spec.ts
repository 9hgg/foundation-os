import { DEFAULT_FACILITY_SELECTION_MODAL_DATA, FacilitiesSelectionModalComponent } from './facilities-selection-modal.component';

describe('facilities-selection-modal.component', () => {
	describe('DEFAULT_FACILITY_SELECTION_MODAL_DATA', () => {
		it('is exported', () => {
			expect(DEFAULT_FACILITY_SELECTION_MODAL_DATA).toBeDefined();
		});
	});

	describe('FacilitiesSelectionModalComponent', () => {
		it('is exported', () => {
			expect(FacilitiesSelectionModalComponent).toBeDefined();
		});
	});
});
