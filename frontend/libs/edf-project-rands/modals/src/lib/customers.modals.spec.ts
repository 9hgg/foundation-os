import { CustomersModals } from './customers.modals';

describe('customers.modals', () => {
	describe('CustomersModals', () => {
		it('is exported', () => {
			expect(CustomersModals).toBeDefined();
		});
	});
});
