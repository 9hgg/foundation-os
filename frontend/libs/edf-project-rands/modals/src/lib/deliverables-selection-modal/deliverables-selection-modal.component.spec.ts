import { DEFAULT_DELIVERABLE_SELECTION_MODAL_DATA, DeliverablesSelectionModalComponent } from './deliverables-selection-modal.component';

describe('deliverables-selection-modal.component', () => {
	describe('DEFAULT_DELIVERABLE_SELECTION_MODAL_DATA', () => {
		it('is exported', () => {
			expect(DEFAULT_DELIVERABLE_SELECTION_MODAL_DATA).toBeDefined();
		});
	});

	describe('DeliverablesSelectionModalComponent', () => {
		it('is exported', () => {
			expect(DeliverablesSelectionModalComponent).toBeDefined();
		});
	});
});
