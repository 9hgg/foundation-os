import * as SUT from './deliverables-selection-modal.component';

describe('deliverables-selection-modal.component', () => {
  it('should be defined', () => {
    expect(SUT).toBeDefined();
  });
});
