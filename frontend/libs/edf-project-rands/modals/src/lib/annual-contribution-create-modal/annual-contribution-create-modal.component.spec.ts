import * as SUT from './annual-contribution-create-modal.component';

describe('annual-contribution-create-modal.component', () => {
  it('should be defined', () => {
    expect(SUT).toBeDefined();
  });
});
