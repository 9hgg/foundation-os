import { fixWebmDuration } from './webm.utils';

describe('webm.utils', () => {
	describe('fixWebmDuration', () => {
		it('is exported', () => {
			expect(fixWebmDuration).toBeDefined();
		});
	});
});
