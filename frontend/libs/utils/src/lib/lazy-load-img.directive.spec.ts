import { LazyLoadImgDirective } from './lazy-load-img.directive';

describe('lazy-load-img.directive', () => {
	describe('LazyLoadImgDirective', () => {
		it('is exported', () => {
			expect(LazyLoadImgDirective).toBeDefined();
		});
	});
});
