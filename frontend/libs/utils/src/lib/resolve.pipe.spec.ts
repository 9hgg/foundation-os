import { ResolveFinalUrlPipe } from './resolve.pipe';

describe('resolve.pipe', () => {
	describe('ResolveFinalUrlPipe', () => {
		it('is exported', () => {
			expect(ResolveFinalUrlPipe).toBeDefined();
		});
	});
});
