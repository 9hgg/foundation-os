import { DragAndDropService } from './drag-and-drop.service';

describe('drag-and-drop.service', () => {
	describe('DragAndDropService', () => {
		it('is exported', () => {
			expect(DragAndDropService).toBeDefined();
		});
	});
});
