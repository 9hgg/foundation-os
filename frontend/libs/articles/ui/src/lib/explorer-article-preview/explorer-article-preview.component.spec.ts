import '@angular/compiler';
import * as SUT from './explorer-article-preview.component';

describe('explorer-article-preview.component', () => {
	it('should be defined', () => {
		expect(SUT).toBeDefined();
	});
});
