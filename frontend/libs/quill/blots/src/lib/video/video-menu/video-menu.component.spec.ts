import { VideoMenuComponent } from './video-menu.component';

describe('video-menu.component', () => {
	describe('VideoMenuComponent', () => {
		it('is exported', () => {
			expect(VideoMenuComponent).toBeDefined();
		});
	});
});
